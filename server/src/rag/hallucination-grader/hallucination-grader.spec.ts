import { HallucinationGrader } from './hallucination-grader';

describe('HallucinationGrader', () => {
  it('should be defined', () => {
    expect(new HallucinationGrader()).toBeDefined();
  });
});
